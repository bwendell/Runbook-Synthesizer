/**
 * REST API resources (Helidon handlers).
 *
 * <p>HTTP endpoints for the Runbook-Synthesizer service:
 *
 * <ul>
 *   <li>/api/v1/alerts - Alert ingestion
 *   <li>/api/v1/runbooks/sync - Trigger re-indexing
 *   <li>/api/v1/webhooks - Webhook configuration
 *   <li>/health - Health check
 * </ul>
 */
package com.oracle.runbook.api;
